# Secrets — never commit. (The real .env lives one level up, outside this repo.)
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# BareCode's own cache
.barecode/

# Test/demo scratch space
tmp/
fixtures/generated/

# macOS
.DS_Store

# Build output: produced by `make build`, not committed.
dist/

# Explainer source is tracked; regenerate the PDF with:
#   chrome --headless --no-pdf-header-footer --print-to-pdf=docs/BareCode-Explainer.pdf docs/EXPLAINER.html
